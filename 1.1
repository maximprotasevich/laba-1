#include <stdio.h>
#include <math.h>


int main()


{
    int a = 0;
    float    S, h;
    printf("enter side ");
    scanf("%d",&a);
    S = a * a * sqrt(3) / 4;
    h = a * sqrt(3) / 2;
    printf("Ploshad treygolnika = %.3f\n",S);
    printf("Vysota treygolnika = %.3f\n",h);
    return 0;
}
